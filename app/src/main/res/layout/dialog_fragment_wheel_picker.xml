<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:aigestudio="http://schemas.android.com/apk/res-auto"
    >
    <data/>
    <RelativeLayout
        android:clickable="true"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        >

        <ImageView
            android:id="@+id/ivCloseWheelPicker"
            android:layout_width="@dimen/size_40"
            android:layout_height="@dimen/size_40"
            android:src="@mipmap/done_old"
            android:layout_alignParentEnd="true"
            android:elevation="@dimen/margin_5"
            android:layout_marginTop="@dimen/margin_30"
            android:layout_marginEnd="@dimen/margin_10"
            android:padding="@dimen/margin_2"
            />

        <com.aigestudio.wheelpicker.WheelPicker
            android:id="@+id/wheel_picker"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            aigestudio:wheel_atmospheric="true"
            aigestudio:wheel_curved="true"
            aigestudio:wheel_cyclic="false"
            aigestudio:wheel_item_text_color="@color/colorSearch2"
            aigestudio:wheel_item_text_size="80sp"
            aigestudio:wheel_selected_item_text_color="@color/white"
            aigestudio:wheel_data="@array/CountryCodes"
            android:layout_centerInParent="true"
            android:paddingStart="@dimen/margin_25"
            android:paddingEnd="@dimen/margin_25"
            />

    </RelativeLayout>

</layout>